services:
  alfebot:
    build:
      context: ..
      dockerfile: docker/Dockerfile

    container_name: alfebot

    restart: unless-stopped

    env_file:
      - ../.env

    deploy:
      resources:
        limits:
          memory: 512M